name: workflow cmd

on: [push]  

jobs:
  workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: workflow command
        run: |
          echo "::group::my group message"
          echo "msg 1"
          echo "::endgroup::"
  